#Создаем новую таблицу books, т.к. старая была перйменована в book_old
#Можно было и не создавать, а удалить лишние колонки в старой таблице, но я решил пойти по пути миграции данных

CREATE TABLE books(
id SERIAL PRIMARY KEY,
title TEXT NOT NULL
);
